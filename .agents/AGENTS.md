# Agent Rules for Roshan-Adhikari/T29-quick-access

- **Automatic GitHub Sync**: Whenever code modifications are successfully completed locally, the agent must automatically stage, commit, and push the changes to GitHub (`origin/main`) using a descriptive commit message. Do not ask for user permission before running `git add`, `git commit`, and `git push`.
